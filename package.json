{
  "name": "zimotollm-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve --port=8089",
    "build": "node scripts/build.mjs",
    "test": "npm run build && node scripts/check.mjs"
  },
  "engines": { "node": ">=22" },
  "devDependencies": { "@11ty/eleventy": "3.1.6" }
}
